# CONTEXT — 2026-05-05 early hours (P1 bug-fix bundle, run #5 in flight)

> **Bootstrap file**. Read this first after `/clear`. Then read `prds/MASTER_PLAN.md` (header + Recommended next move + slot #0 row).
> Delete this file (and `CONTEXT_2026-05-04.md`) once run #5 closes the bundle and you've caught up; they're session-bridge artifacts.

## TL;DR

- Bundle session `2026-05-04-f416c6cc` is **RUNNING** in fresh tmux `pipeline-fresh-f416c6cc` (launched 2026-05-05 01:28 local).
- Backend = **claude-only Path X** (manager + workers). Codex-spark proven unreliable for the manager role (R12 materialized; manager hallucinated backend flips). Worker subprocesses also stay on claude until slot 1o ships.
- **14 Done / 48 Todo.** All 14 Done armored with `completion_commit:` SHAs (phantom-Done-proof).
- **R-CNAR-1 part 2 cap-split fix** (`6be334b1`, 2026-05-05) is live — global vs per-ticket caps now independent. `state.max_iterations=500` GLOBAL, per-ticket budgets stay tier-driven. Was the actual root cause of "pipeline keeps dying after 10 iters" — see `extension/CLAUDE.md` trap-door entry.
- **89 unpushed commits**, 0 tagged. v1.66.0 still GitHub-Latest with poison content. Bundle closer (R-CLOSER-1) ships v1.70.0.

## Quick re-verify after `/clear`

```bash
cd /Users/gregorydickson/loanlight/pickle-rick/pickle-rick-claude
git status --short                                            # expect clean (modulo MASTER_PLAN.md doc churn)
git rev-list --count @{u}..HEAD                               # ~89

SESSION=~/.local/share/pickle-rick/sessions/2026-05-04-f416c6cc
jq '{step,active,iteration,max_iterations,backend,current_ticket,exit_reason}' "$SESSION/state.json"
# expect: active=true, backend=claude, max_iterations=500, exit_reason=null while running

tmux list-sessions | grep f416                                # pipeline-fresh-f416c6cc
tmux attach -t pipeline-fresh-f416c6cc                        # Ctrl+B 1 = monitor, Ctrl+B D = detach

# md5 parity sanity
for f in extension/types/index.js extension/services/state-manager.js extension/services/pickle-utils.js extension/bin/spawn-morty.js extension/bin/mux-runner.js; do
  cmp -s "$f" "$HOME/.claude/pickle-rick/$f" && echo "✓ $f" || echo "✗ $f"
done
```

## Where things stand

### Bundle slot #0 — `prds/p1-bug-fix-bundle-2026-05-04.md`

- 62 atomic tickets refined via 3-cycle team (claude). 14 Done / 48 Todo.
- **Section A keystones COMPLETE** (cross-backend leak class, slot 1j):
  - `7793a11` R-XBL-1 — `worker_spawn_backend_resolved` activity event with source tracking
  - `9437b0c` R-CNAR-1 — TICKET_TIER_BUDGETS raised to `{trivial:5/5m, small:10/10m, medium:30/20m, large:60/80m}`; xlarge dropped
  - `817e73c` R-XBL-2 — read-side SoT (StateManager.read at 4 spawn sites) + `--backend` CLI flag + `worker_spawn_backend_override` event
  - `a3641e3` R-XBL-2 worker overlay — adds `assertBackendPreSpawn` import path
  - `616f474` R-XBL-2b — spawn-gate-remediator inheritance audit event
  - `95f2c37` R-XBL-3 — pre-spawn assertion + `state.flags.backend_flip_reason` carve-out
  - `7d81aad6` R-XBL-6 — `audit-worker-backends.ts` backfill audit script
  - `044a8d42` R-XBL-7 — env-poison defense integration test
  - `a2690794` R-XBL-7b — actual-session-2026-05-03-7d9ee8cc reproducer test
  - `7ef0c041` R-XBL-8 — trap-door entry for spawn-morty backend resolution
  - `8c692f2e` R-XBL-9 — refinement-team prompt schema reference to new events
  - `50c43b9c` AC-XBL-08 — manager-relaunch backend-flip regression test
  - `e5d64089` AC-EVENT-PAYLOAD-01 — `activity-events.schema.json` + parametrized validators
  - `ee2ae138` R-XBL-5 — `subtool_backend_override` event from sub-tool command files (send-to-morty, codex-rescue)
- **Section B partial:** `cd35ae82` R-BUNDLE-CLEANUP, `6f1a5486` R-DTS-1 regression assertions.
- **Section B/C/D/E remainder + closer = 48 Todo.** Run #5 should ride to completion with cap-split fix (~5 hrs estimated).
- Section A residual not in commits above: R-XBL-7c (regression test for xbl-7b assertion), if any. Review on close.

### Run history

| Run | Started | Ended | Outcome | Notes |
|---|---|---|---|---|
| #1 | 2026-05-04 13:23 | crash | codex-spark backend, hermes flip mid-run, never validated | filed slots 1o/1p/1q forensics |
| #2 | 2026-05-04 16:59 | 17:28 (28m) | circuit-tripped (per-ticket cap=4) | shipped 4 commits; manager hallucinated hermes flip; phantom-Done reverted 3 truly-done tickets |
| #3 | 2026-05-04 17:42 | 22:30 (~5h) | iteration_cap_exhausted (cap=10 — the bug) | shipped 9 atomic commits; claude-only manager solid; revealed cap-conflation bug |
| #4 | 2026-05-05 00:57 | 01:25 (~28m) | iteration_cap_exhausted (cap=10) | confirmed cap-conflation bug; minor progress |
| **#5** | **2026-05-05 01:28** | **(running)** | (in flight) | **First run with cap-split fix `6be334b1`. max_iterations=500 GLOBAL, per-ticket=tier ceiling.** |

## NEW findings (2026-05-04 → 2026-05-05) — slots 1o..1r

| Slot | PRD | Severity | Filed | Status |
|---|---|---|---|---|
| 1o | (proposed) `p1-worker-backend-split-from-manager.md` | P1 | 2026-05-04 evening from run #2 | Not drafted. ~15 LOC: optional `state.worker_backend` field; spawn-morty consults before falling back to `state.backend`. Manager=claude, worker=codex-spark hybrid. Removes F1 hallucination class. |
| 1p | (proposed) `p2-codex-spark-worker-completion-commit-contract-violation.md` | P2 | 2026-05-04 evening | Not drafted. codex-spark workers commit to git but skip `completion_commit:` frontmatter; phantom-Done reverts truly-done. Mitigation = post-commit wrapper auto-fills field, or watcher cross-checks git log. |
| 1q | (proposed) `p2-install-sh-types-index-stale-on-fast-reinstall.md` | P2 | 2026-05-04 evening | Not drafted. install.sh first-run left deployed `extension/types/index.js` md5-mismatched (8 events missing); state-manager rejected events for entire run. Fix: post-rsync md5-parity probe + tsc force-rebuild. |
| 1r | `prds/anatomy-park-judge-unreachable-on-worker-convergence.md` | P1 | 2026-05-05 from `pipeline-2026-05-04-8aecd4c7` | Drafted. Sibling of v1.63.0 finalizer-history crash. `microverse-runner.js:validateWorkerConvergenceHistory()` (lines 447-474) returns `judge_unreachable` when `metric_type='none'`. ~10 LOC fix; 6 ACs. Skip guard when metric.type === 'none'. Live repro: pipeline shipped 21/21, anatomy-park converged, runner threw it away, szechuan-sauce skipped. |

## Watchpoints during run #5

1. **`worker_spawn_backend_resolved` events** should all show `backend=claude source=state`. Any other source = R-XBL-3 should fire pre-spawn. If a spawn somehow proceeds with mismatch — investigate.
2. **`iteration_cap_exhausted` exit_reason** — if it fires, check the log line:
   - `per-ticket budget (N/M, tier=X) exhausted on ticket <id>` → per-ticket fired (working as designed; one ticket stuck)
   - `global iteration cap (N/M) exhausted` → global fired (operator's cap reached after 500 manager iterations)
   - Pre-fix would have always shown the per-ticket-budget value as if it were global; new logging distinguishes.
3. **Phantom-Done reverts** — claude follows the `completion_commit:` contract reliably, so this shouldn't happen for run #5 commits. Older Done tickets (already armored) are also safe.
4. **Stop-hook orphan-shadow** (slot 1n) — stale `current_sessions.json` mappings can cause "Loop Active" feedback to point at the wrong session. I cleared the bundle's mapping at run #5 launch. Verify with `cat ~/.local/share/pickle-rick/current_sessions.json`.
5. **`bundle/ac-dr-02.json`** — test debris re-generated by `verify-recapture-fired` on every test run. Bundle's R-BUNDLE-CLEANUP (`cd35ae82`) gitignored it, so this should be moot, but `git status` may show drift if a test re-creates it pre-gitignore.

## Don't forget

- v1.66.0 is **still GitHub-Latest with poison content**. Tag v1.70.0 only after this round closes (closer ships it via `gh release create --latest`).
- Slot 1f R-ICP-7 regression test still owed (R-ICP-1..6 already-shipped on session `2026-05-03-7d9ee8cc`, commits `434e33d a7ed2a9 2de4c24` unpushed).
- Activity-log shape on session `2026-05-03-7d9ee8cc` is degraded — `jq -r '.activity[].timestamp'` returns nulls. Forensic on that session via per-ticket worker logs and `tmux_iteration_*.log`, not state.json activity.
- Slot 1o (worker_backend split) is the architectural follow-up to enable hybrid claude-manager + codex-spark-worker mode. Saves cost on worker spawns. Not blocking; queue post-bundle.
- Babysit script `~/.local/share/pickle-rick/sessions/2026-05-04-f416c6cc/babysit.sh` is no longer running (killed before run #5 launch). With cap-split fix in place, run #5 should not need auto-relaunch.

## Key file refs

- MASTER_PLAN: `prds/MASTER_PLAN.md` (header + Recommended next move + slot #0/1f/1g/1j/1o..1r rows)
- This bootstrap: `CONTEXT_2026-05-05.md`
- Prior bootstrap (still relevant): `CONTEXT_2026-05-04.md`
- Bundle session: `~/.local/share/pickle-rick/sessions/2026-05-04-f416c6cc/`
  - `state.json` — live state (active, step, iteration, current_ticket, max_iterations, backend, ...)
  - `pipeline.json` — phases array + targets + per-phase configs (backend=claude as of run #5)
  - `pipeline-status.json` — phase progress
  - `circuit_breaker.json` — no-progress / same-error breaker state
  - `<ticket>/linear_ticket_<ticket>.md` — per-ticket frontmatter (status, completion_commit, complexity_tier)
  - `<ticket>/worker_session_<pid>.log` — per-spawn worker output
  - `tmux_iteration_*.log` — per-iteration manager NDJSON
  - `babysit.log` — auto-relaunch wrapper log (run #4 era; not active for run #5)
- Bundle PRD: `prds/p1-bug-fix-bundle-2026-05-04.md` (Section Notes section has run-#2 forensics)
- Cap-split fix landed in: `extension/src/bin/mux-runner.ts` (applyTicketTierBudget L527-545, runMuxLoop cap-check L2575-2615) + `extension/tests/mux-runner-cap-split.test.js`

## Skills used in this session

- 1 `git commit` for the cap-split fix (`6be334b1`)
- 1 `bash install.sh` re-deploy (md5 parity restore)
- 1 `/pickle-pipeline` invocation (rerouted to --resume on existing session)
- Multiple Edit/Read on PRDs + MASTER_PLAN.md
- 1 `babysit.sh` write (auto-relaunch wrapper, killed before run #5)
- 1 Explore agent (PRD inventory cross-reference)

— Pickle Rick out. *belch*
