# CONTEXT — 2026-05-03 (post-mega-bundle session)

> **Bootstrap file**. Read this first after `/clear`. Then read `prds/MASTER_PLAN.md`.
> Delete this file once you're caught up; it's a session-bridge artifact.

## Where we left off (what happened across the last session)

Two big bundles shipped in code; **zero releases tagged on GitHub**. Two new bug classes surfaced and got logged.

### P0 deploy-reversion bundle — 30/30 tickets Done, NO release tag
- Session: `~/.local/share/pickle-rick/sessions/2026-05-02-ad240987/`
- Refined PRD: `prds/p2-bundle-deploy-reversion-and-gate-baseline-diagnostic.md`
- Sections A (14) + B (2) + C (2) + D (3) + infra (2) + wiring + 4 hardening + closer + scheduled-soak.
- Closer DEFERRED live `gh release create v1.68.0` because env crontab restriction.
- Cycle 3 codebase analyst's verdict: bundle is **over-engineered** — only AC-DR-04c (release-gate.sh) is the actual fix.

### P1 strip-back PRD drafted (still unrun)
- `prds/p1-strip-excessive-defense-deploy-reversion.md` — trims ~480 LOC of cron sampler + mux-runner pre-flight + scheduled-soak. Got absorbed into the mega bundle as Section A (5 tickets) + landed.

### Mega bundle — 34/34 tickets Done, NO release tag, pipeline STUCK
- Session: `~/.local/share/pickle-rick/sessions/2026-05-02-fca7952b/`
- Refined PRD: `prds/p2-mega-bundle-2026-05-02-pm.md`
- Composes 6 source PRDs: strip + multi-repo-state-drift + tool-error-retry + smart-handoff + hermes + god-fn-phase-2.
- Sections A (5) + B (4) + C (3) + D (6) + E (5) + F (4) + rollup + wiring + 4 hardening + closer.
- Closer ran: bumped pkg.json `1.67.0` → **1.69.0** (commit `bdc775f`). Skipped 1.68.0 entirely.
- gh release for v1.69.0 **NOT published** (likely DEFERRED again).
- **Pipeline session is STUCK** — see new bug below.

### Trap-door fixes (committed, `1bdaffc`)
- Split `extension/CLAUDE.md` lines 9, 13 into single-triple bullets per `tests/trap-door-conformance.test.js` contract.
- Created two missing referenced test files: `extension/tests/mux-runner-state-iteration.test.js` (4 tests) + `extension/tests/get-extension-root-fallback.test.js` (3 tests).

## NEW bug PRDs logged this session

### `prds/p1-deployed-pkgjson-version-only-revert.md` (P1)
NEW class of deploy-reversion. Distinct from the original tarball-rsync revert:

| Field | Source | Deployed | Same? |
|---|---|---|---|
| `extension/package.json:version` | `1.69.0` | reverts to `1.64.0` periodically | ❌ |
| `extension/bin/check-update.js` (md5) | matching | matching | ✅ |
| `pickle_settings.json:auto_update_enabled` | `false` | `false` | ✅ |

ONLY the version field flips. Kill-switch is engaged. Spawn rate-limit (A.9 from P0 bundle) holding at 1065. 5 hypotheses (test pollution, worktree drift, npm postinstall, cron, gh-tarball partial extract). Diagnostic-first: **R-PJV-1** = `fs_usage`/`lsof` to identify writer before adding more defense.

### `prds/p2-codex-manager-empty-queue-spin.md` (P2)
Codex manager doesn't recognize all-tickets-Done state. Spins bootstrap loop instead of emitting `<promise>EPIC_COMPLETED</promise>`. Pipeline-runner stays stuck in PHASE 1/4 PICKLE indefinitely. Distinct from D.3 of mega bundle (which fixes "what failed in iter N", not "queue emptiness"). Fix: mux-runner scans queue at iteration_start and synthesizes completion promise when all `Done`.

## Working tree state when this file was written

```
Branch: main
Working tree: CLEAN (PRDs + MASTER_PLAN about to commit)
Source pkg.json: 1.69.0 (commit bdc775f)
Deployed pkg.json: drifts every 30-60 min — currently OK at 1.69.0 after last redeploy
Latest GitHub release: v1.66.0 (BROKEN/poison; v1.67/68/69 untagged)
Commits ahead of origin: ~120
```

## Active sessions / processes

- **Pipeline tmux session `pipeline-fca7952b`** is STILL RUNNING — codex manager spinning bootstrap loop on empty queue. Bundle work is shipped (commits in main); the spin is purely the empty-queue bug. Safe to kill: `tmux kill-session -t pipeline-fca7952b`. P0 bundle session also retained but inactive.
- **NO cron babysits armed** — both `a3a6970f` (30min) and `2ba30074` (1h) were cancelled.

## Recommended next actions (after `/clear`)

### IF the goal is "ship v1.69.0":

1. **Decide the release ceremony order**. Options:
   - (a) Tag v1.69.0 directly (high risk: v1.66.0 still GitHub-Latest with poison; v1.69.0 displaces it but old auto-updaters may have v1.66.0 cached).
   - (b) First `gh release delete v1.66.0` (irreversible — requires `bundle/pre-deletion-archive/` from P0 bundle as forensic backup), then tag v1.69.0.
   - (c) Run `bash bin/release-gate.sh --pre-tag v1.69.0` → if green, `gh release create v1.69.0` → `bash bin/release-gate.sh --post-tag v1.69.0` → `bash install.sh`.
2. **Kill the stuck pipeline** before tagging: `tmux kill-session -t pipeline-fca7952b`. Pipeline phases 2/4 (anatomy-park) and 3/4 (szechuan-sauce) never ran for the mega bundle — operator can run them manually post-release if desired.
3. **Verify deploy parity post-tag** with the operator workaround:
   ```bash
   SRC_V=$(jq -r .version /Users/gregorydickson/loanlight/pickle-rick/pickle-rick-claude/extension/package.json)
   DEP_V=$(jq -r .version $HOME/.claude/pickle-rick/extension/package.json)
   [ "$SRC_V" = "$DEP_V" ] || bash install.sh
   ```

### IF the goal is "investigate the new bugs first":

1. `p1-deployed-pkgjson-version-only-revert.md` R-PJV-1 — run `sudo fs_usage -w -f filesys 2>&1 | grep "extension/package.json"` for 60+ min after `bash install.sh` to identify the writer.
2. `p2-codex-manager-empty-queue-spin.md` R-EQ-1 — patch `extension/src/bin/mux-runner.ts` to scan ticket queue at iteration_start and synthesize `EPIC_COMPLETED` when all Done.

### IF the goal is "babysit / continue running pipelines":

The deploy-reversion bug (pkg.json-only revert) bites every 30-60 min. Operator must run `bash install.sh` + restore typescript symlink (`ln -sf $REPO/extension/node_modules/typescript ~/.claude/pickle-rick/extension/node_modules/typescript`) on every drift. A `/loop 30m bash install.sh && [ts symlink restore]` cron is the operational shield.

## Don't forget

- v1.66.0 is **still GitHub-Latest** with poison content (tarball ships v1.64.0 code per the procedural tag-pre-bump bug). Anything that fetches "latest" gets the poison.
- **NO CRON BABYSIT IS RUNNING.** If you launch another long pipeline, re-arm.
- The `~/.claude/pickle-rick/extension/node_modules/typescript` symlink keeps disappearing after `bash install.sh`. Restore it after every install.
- Pre-existing trap-door failures in `extension/CLAUDE.md` are FIXED in this session (1bdaffc). Future runs no longer need workarounds.
- The `state.flags.skip_readiness_reason` bypass was set on the mega bundle session because the readiness-gate-manifest bug (Section D.1-D.3 fix) wasn't deployed yet at launch time. Future sessions don't need this bypass once v1.69.0 deploys.

## Key file refs

- MASTER_PLAN: `prds/MASTER_PLAN.md`
- Mega bundle session: `~/.local/share/pickle-rick/sessions/2026-05-02-fca7952b/`
- P0 bundle session: `~/.local/share/pickle-rick/sessions/2026-05-02-ad240987/`
- Refined mega PRD: `prds/p2-mega-bundle-2026-05-02-pm.md`
- New bug PRDs:
  - `prds/p1-deployed-pkgjson-version-only-revert.md`
  - `prds/p2-codex-manager-empty-queue-spin.md`
- Strip PRD (absorbed into mega bundle Section A): `prds/p1-strip-excessive-defense-deploy-reversion.md`

## Skills used in last session

- `/pickle-pipeline --backend codex --max-iterations 1500 --max-time 0 --acknowledge-undersized` (twice, both stuck on phase 1/4 closer)
- `/pickle-refine-prd` (called inline by /pickle-pipeline)
- `/loop 30m` (P0 babysit) and `/loop 1h` (mega bundle babysit) — both cancelled
- 2-agent team (general-purpose) for trap-door fixes
