# CONTEXT — 2026-05-06 PM, quick-refine pipeline live

> Bootstrap after `/clear`. Read this first. Supersedes `CONTEXT_2026-05-06_path-A.md` and `CONTEXT_2026-05-05_post-merge.md`. Both retained on disk for forensics; safe to delete.

## What's running RIGHT NOW

**Session `pipeline-e0834dcd`** — `/pickle-pipeline --no-refine --backend codex` driving 9 atomic implementation tickets through pickle → anatomy-park → szechuan-sauce phases.

```
SESSION_ROOT  /Users/gregorydickson/.local/share/pickle-rick/sessions/2026-05-06-e0834dcd
tmux          tmux attach -t pipeline-e0834dcd
state         active=true, codex_model=gpt-5.4, current_ticket=6e80b612 (slot 1r/1s)
backend       codex / gpt-5.4 (NOT spark — usage limit hit, switched mid-run)
flags         skip_readiness_reason + skip_ticket_audit_reason both set (R-RTRC-* / R-TAQ-3 trap-door bypass — forward-created refs are intentional)
```

### Tickets

| Order | ID | Slot | Status | Source PRD |
|------:|----|------|--------|-----------|
| 10 | `09969d52` | 1u | ✅ Done `162c226f` | manager stop-hook nudge cadence (R-MSCN-1..6) |
| 20 | `bb08867f` | 1t | ✅ Done `723cb99c` | wall-clock cap removal default (R-NTC-1..10) |
| 30 | `6e80b612` | 1r/1s | 🔄 In flight | anatomy-park judge_unreachable + szechuan timeout-as-stall (R-AJUR-* + R-MJU-*) |
| 40 | `edae8fa8` | 1o | Todo | worker_backend split (R-WBS-1..6) — foundation for 1p |
| 50 | `167fcaf9` | 1p | Todo | codex-spark completion_commit auto-fill (R-CCC-*) — depends on 1o |
| 60 | `6edd8868` | 1n | Todo | stop-hook orphan-shadow (R-SHB-1..6) |
| 70 | `1a11461c` | 1g | Todo | R-CNAR-7 cap-check guard residual (partial-shipped via 96ce65cf; targets 4 remaining gaps) |
| 80 | `1e821336` | 1m | Todo | pipeline-runner dirty-tree guard (R-PDT-1..4) |
| 90 | `91601dd7` | 1d | Todo | test flakes — council-publish hung-timeout + scope-resolver (R-TF-1..4) |

### Resume command if `/clear` happens mid-run

```bash
# tmux session still alive:
tmux attach -t pipeline-e0834dcd

# tmux killed but session dir intact:
tmux kill-session -t pipeline-e0834dcd 2>/dev/null
tmux new-session -d -s pipeline-e0834dcd -c /Users/gregorydickson/loanlight/pickle-rick/pickle-rick-claude
tmux send-keys -t pipeline-e0834dcd:0 \
  "bash '/Users/gregorydickson/.local/share/pickle-rick/sessions/2026-05-06-e0834dcd/launch.sh' '/Users/gregorydickson/.local/share/pickle-rick/sessions/2026-05-06-e0834dcd'" Enter
```

### Cancel command

```bash
tmux kill-session -t pipeline-e0834dcd
pkill -9 -f 'pipeline-runner.js.*e0834dcd|mux-runner.js.*e0834dcd|spawn-morty.js'
```

## Verify before acting

```bash
cd /Users/gregorydickson/loanlight/pickle-rick/pickle-rick-claude
git rev-parse --abbrev-ref HEAD                       # expect: main
git rev-list --count origin/main..HEAD                # ≥39 (will grow as tickets land)
git log --oneline -12                                 # see slot 1u (162c226f), 1t (723cb99c), path A meta-tickets, slot 1q
git status --short                                    # expect: clean (workers commit atomically)
jq '.active, .step, .current_ticket, .iteration, .codex_model' \
  /Users/gregorydickson/.local/share/pickle-rick/sessions/2026-05-06-e0834dcd/state.json
```

## What just happened (2026-05-05 → 2026-05-06)

### Shipped post-v1.70.0 (in commit order)

| Commit | Ticket | What |
|--------|--------|------|
| `244b4c51` | — | Remove audit-canary-flip from gate (CLAUDE.md ×2 + check-wired.sh + 2 parity tests + 2 workflow YAMLs) |
| `49e0ff84` | — | trap-door-conformance: add test-file refs to 5 grep-only ENFORCE entries (62/62 pass) |
| `f6909d78` + `1949c6a4` + `efe0e961` | slot 1q | install.sh deploy-parity (R-ITS-1..4: force-rebuild + md5 probe + activity event + trap-door + tests). Follow-ups: peripheral count assertion 11→12; force-rebuild made TS-derived only (preserves JS-only utilities). Shipped via `/pickle-tmux` in 99 min. |
| `80430696` | — | Mega bundle PRD composed (Section CF carry-forwards + slot 1q ALREADY-SHIPPED) |
| `68d9c1bf` | path-A 1/4 | Lift section lead requirement ACs from peer PRDs into bundle PRD with file:line anchors |
| `62b34588` | path-A 2/4 | Split AC-06 into 06a (dispositions) + 06b (path-decision) |
| `0b16a707` + `48047f56` | path-A 3/4 | Register 6 new activity events through full quartet |
| `c5896828` | — | Master plan + path-A bootstrap doc |
| `34146d6e` | — | File `/pickle-quick-refine` command PRD (validated workflow) |
| `162c226f` | slot 1u | Manager stop-hook idle-backoff (R-MSCN-1..6) |
| `723cb99c` | slot 1t | Wall-clock cap default-off (R-NTC-1..10) + gpt-5.4 model switch |

### Path A → quick-refine pivot

1. Mega bundle PRD's first refinement pass produced only 5 meta-tickets (PRD-shape fixes), not implementation tickets — the bundle delegated AC bodies to peer PRDs via `composes:` block. **Path A** ran 4 PRD-prep meta-tickets to fix the structure; 3 shipped, 1 skipped on hard fast-failure loop.
2. Re-refinement after path A produced 14 deduped tickets (~6 unique work areas), missing 5 of 9 source PRDs.
3. **Pivoted to quick-refine**: spawned 9 parallel `Agent` calls, each authoring 1 ticket from 1 source PRD in ~2 min wall-clock total. Validated as `/pickle-quick-refine` PRD at `prds/p2-abbreviated-refine-command.md`.
4. Pipeline halted twice on readiness gate + ticket-audit gate (forward-created refs to test files / paths the tickets create). Set `state.flags.skip_readiness_reason` + `skip_ticket_audit_reason` per R-RTRC-* / R-TAQ-3 trap-door bypass. Pipeline now running cleanly.
5. **Spark usage limit hit** mid-pipeline. Switched `gpt-5.3-codex-spark` → `gpt-5.4` in 4 places: `.codex/config.toml`, `pickle_settings.json` (source + deployed), `state.codex_model`. Reset 7 Failed tickets → Todo and restarted on gpt-5.4.
6. **9 review agents fixed path-drift + forward-created annotations** across all 9 tickets in ~3 min wall-clock total. ACs verbatim from source PRDs; `<!-- audit: 7-class checked 2026-05-06 -->` added to every Conformance Check.

## What's next

1. **Wait for pipeline-e0834dcd to complete** — 7 of 9 tickets remaining. ETA varies; with codex/gpt-5.4 at ~30-90 min/ticket = 4-10h.
2. **Anatomy-park + szechuan-sauce phases** auto-run after pickle phase completes (per pipeline.json).
3. **Carry-forwards from bundle 2026-05-04** still deferred — AC-TAQ-09, R-BUNDLE-1/2/DISPO-1, 5 Section H Wire/Harden/Audit. They don't have standalone PRDs (they're inline in `prds/p1-bug-fix-bundle-2026-05-04.md`). Future batch.
4. **No closer ticket** — local-only scope. After pipeline finishes, operator decides whether to push and release later.

## Known carry-over risks

- **fast-tier deadlock** on `node --test tests/activity-event-payload.test.js` under `npm run test:fast`'s parallel worker pool — standalone runs in 67ms, but the runner pool deadlocks. Workaround: `pkill -9 -f 'node --test'`. Slot 1q's worker hit this; recovery worked. Future pipeline workers may hit it.
- **Mux-runner orphan accumulation** — pickle's monitor watchdog (R-MWR-1) respawns mux-runners attached to active tmux sessions. Stale orphans after sessions end consume some resources but are usually harmless. If they balloon (>20), `pkill -9 -f 'mux-runner\.js'` between runs is safe.
- **Spark usage limit** — already hit; we're on gpt-5.4. If gpt-5.4 also throttles, edit `.codex/config.toml` model field (one ASCII line: `model = "gpt-5.4"`) plus `pickle_settings.json` `default_codex_model` to whichever model the user prefers. Re-run pickle install.sh ONLY when no active session — R-ITS-5-MIN refuses mid-pipeline install.

## Don't do

- **Don't `git push`** — local-only mode. v1.70.0 is still GitHub-Latest. If pushing later, MERGE_BASE may need pinning to v1.70.0 for audit scope.
- **Don't `bash install.sh` mid-pipeline** — R-ITS-5-MIN refuses; the R-ITS-1/-2 safety net catches cache drift now.
- **Don't hand-edit running-session ticket files** in `~/.local/share/pickle-rick/sessions/2026-05-06-e0834dcd/<hash>/` — the worker is editing them.
- **Don't kick off another sprawling parallel-agent bundle** for ticket-creation while the pipeline is running — the existing 9 tickets are good.

## Key files

- This bootstrap: `CONTEXT_2026-05-06.md`
- Master plan: `prds/MASTER_PLAN.md`
- Mega bundle PRD (in flux): `prds/p1-bug-fix-bundle-2026-05-05.md` (path-A ticket 3097eec3 lifted Local ACs from peer PRDs into it)
- Source PRDs (one per ticket): `prds/p2-manager-stop-hook-nudge-cadence-wastes-turns.md`, `prds/p2-remove-pipeline-wall-clock-time-cap.md`, `prds/anatomy-park-judge-unreachable-on-worker-convergence.md`, `prds/p1-worker-backend-split-from-manager.md`, `prds/p2-codex-spark-worker-completion-commit-contract-violation.md`, `prds/p2-stop-hook-blocks-launcher-of-tmux-bundle-via-orphan-session.md`, `prds/p1-deploy-typescript-symlink-and-cap-no-auto-resume.md`, `prds/p3-pipeline-runner-dirty-tree-guard-blocks-self-cleanup.md`, `prds/p3-test-flakes-council-publish-and-scope-resolver.md`
- Quick-refine command spec: `prds/p2-abbreviated-refine-command.md`
- Active session: `/Users/gregorydickson/.local/share/pickle-rick/sessions/2026-05-06-e0834dcd/`
- Path A session (deactivated): `/Users/gregorydickson/.local/share/pickle-rick/sessions/2026-05-05-b8465d85/`
- 2026-05-04 bundle PRD with Section CF carry-forwards: `prds/p1-bug-fix-bundle-2026-05-04.md`

— Pickle Rick, 2026-05-06 PM. *belch* Three slots SHIPPED, six in the chamber. Two-min decomposition workflow validated and filed. /clear is safe.
