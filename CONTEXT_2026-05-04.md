# CONTEXT — 2026-05-04 PM (post-reliability-bundle, pre-bug-fix-pipeline)

> **Bootstrap file**. Read this first after `/clear`. Then read `prds/MASTER_PLAN.md`.
> Delete this file (and the older `CONTEXT_2026-05-03.md`) once the next pipeline ships and you've caught up; they're session-bridge artifacts.

## TL;DR

- Reliability bundle `2026-05-03-7d9ee8cc` reached **38/38 tickets Done** but pipeline marked `failed` — only the pickle phase ran; **citadel/anatomy-park/szechuan-sauce (phases 2/3/4) did NOT execute**.
- Forensic review found **cross-backend leak** — workers spawned codex despite `state.backend="claude"`. Filed as **slot 1j P1**, integrity-critical, blocks all partial-bundle relaunches.
- **70 unpushed commits, 0 tagged.** No release ceremony pending. User direction 2026-05-04 PM: more bug fixes BEFORE release.
- **Don't relaunch the reliability session as-is** — fix 1j (cross-backend) and 1g (typescript symlink + auto-resume) first.

## Where we left off

### Reliability bundle — 38/38 done, 0/4 phases ran
- Session: `~/.local/share/pickle-rick/sessions/2026-05-03-7d9ee8cc/`
- Source PRD: `prds/p1-reliability-and-test-coverage-bundle-2026-05-03.md` (Sections A-E + hardening)
- Final state: `step=completed active=false exit_reason=failed iteration=10 backend=claude`
- `pipeline-status.json`: `status=failed completed_phases=0 total_phases=4`
- Last ticket `dddee00b` (audit/cross-ref) finished by hand — worker had silent-exit (`worker_session_47876.log` 0 bytes); 4 atomic commits landed it: `5218a33 ab413d7 9381e4d bd81e2e`.
- `bash install.sh` ran successfully 2026-05-04 PM. Typescript-package symlink in place. `node $HOME/.claude/pickle-rick/extension/bin/pipeline-runner.js --help` exit 0.

### What R-ICP-1..6 (slot 1f) bought us
- Pipeline now exits code 3 cleanly on cap-hit with unfinished list.
- Watcher backfills phantom-Done flips with real commits (3-of-3 verified earlier).
- The fix is doing its job — the 38/38 final tally is real, the visible failure modes are downstream of a different bug (1j).

## NEW bugs filed since 2026-05-03 PM

| Slot | PRD | Severity | One-liner |
|---|---|---|---|
| 1e | `p2-refined-tickets-trip-readiness-contract-resolver.md` | P2 | 5 false-positive classes in readiness contract resolver; perf portion shipped |
| 1f | `p1-iteration-cap-and-phantom-done-handshake.md` | P1 | R-ICP-1..6 SHIPPED locally; confirmed working over the full 38-ticket bundle |
| 1g | `p1-deploy-typescript-symlink-and-cap-no-auto-resume.md` | P1 | install.sh doesn't symlink typescript PACKAGE + per-ticket cap halts have no auto-resume |
| 1h | `p2-worker-silent-exit-and-ticket-path-drift.md` | P2 | Worker exits 0 bytes; recurrence note added 2026-05-04 PM (`dddee00b` 0-byte log) |
| 1i | `p1-ticket-authoring-quality-systemic-defects.md` | P1 | 54% defect rate in remaining bundle tickets; refinement-team validation gap |
| **1j** | **`p1-worker-spawns-codex-despite-claude-backend.md`** | **P1** | **NEW — cross-backend leak. 11+ worker logs across 8 ticket dirs ran codex despite `state.backend=claude`. Bundle integrity suspect.** |
| 1k(*) | `p3-monitor-watcher-continuous-auto-respawn.md` | P3 | Monitor panes 1/2/3 went FEED TERMINATED for full iteration; 30s watchdog needed. (*) Filed but not in active queue per user pattern of dropping P3 enhancements from queue rows. |

(*) 1k is a stable PRD reference, not a queue slot — see `prds/p3-monitor-watcher-continuous-auto-respawn.md` directly.

## Working tree state when this file was written

```
Branch: main
Working tree: CLEAN (after committing this CONTEXT file)
Commits ahead of origin: 70 (use `git rev-list --count @{u}..HEAD` — piped `wc -l` may paginate and undercount). Includes prior-session work + bundle work + audit + PRD/context docs.
Source pkg.json: 1.69.0 (carried over from 2026-05-02 mega bundle)
Deployed pkg.json: 1.69.0 (just deployed via install.sh)
Latest GitHub release: v1.66.0 (still poison; v1.67/68/69 untagged as of 2026-05-03)
```

Quick re-verify after `/clear`:

```bash
cd /Users/gregorydickson/loanlight/pickle-rick/pickle-rick-claude
git status --short                                          # expect clean
git rev-list --count @{u}..HEAD                             # expect ~70 (piped wc -l may paginate)
ls -la $HOME/.claude/pickle-rick/extension/node_modules/typescript  # expect symlink to repo
SESSION=~/.local/share/pickle-rick/sessions/2026-05-03-7d9ee8cc
jq '{step,active,exit_reason}' $SESSION/state.json          # expect completed/false/failed
```

## Active sessions / processes

- **Pipeline tmux session `pipeline-7d9ee8cc`** — alive, last seen with all 4 monitor panes running `node` (panes self-revived earlier today after FEED TERMINATED — see slot 1k PRD for the gap). Pipeline-runner subprocess is gone; the session is just monitor + a shell at this point. Safe to kill: `tmux kill-session -t pipeline-7d9ee8cc`. **DO NOT bash launch.sh on this session** until 1j ships.
- **NO cron babysits armed** — none planned for the bug-fix work.
- **Codex usage limit** — resets 2026-05-05 00:31 local. State.backend was flipped to `claude` on this machine. The 1j bug means flipping is unreliable — assume codex may still be invoked by some worker until 1j ships.

## Recommended next actions (after `/clear`)

The user's direction 2026-05-04 PM: **"more bug fixes before release."** Order of attack:

### Tier 1 — bundle-blocking, integrity-critical
1. **1j cross-backend leak** — diagnostic-first (R-XBL-1: log resolved-backend + source at every spawn). Ship R-XBL-2/3 (single-source-of-truth + mismatch assertion) once H1-H4 narrowed. R-XBL-6 audit script gives ground truth on the 11+ already-affected workers.
2. **1g typescript symlink + auto-resume** — A first (deploy gap, one-line install.sh fix), B second (per-tier cap bump + auto-resume daemon). Without B, 38-ticket bundles need 3-7 manual relaunches even when everything else works.

### Tier 2 — quality / friction
3. **1i ticket-authoring quality** — refinement-team validation block + audit-ticket-bundle.js mux-runner pre-flight. Subsumes parts of 1h.
4. **1h worker silent-exit** — RC-1 may be a degenerate case of 1j (R-XBL-6 audit disambiguates). RC-2 (refinement path drift) is real and orthogonal.
5. **1e readiness contract resolver false positives** — the slow part shipped (cache + computeOneHop skip), the false-positive part is open.

### Tier 3 — operator ergonomics
6. **1k monitor watcher continuous auto-respawn** (P3) — bundle behind anything that ships first.

### Suggested first move
Use `/pickle-prd` or `/pickle-pipeline` to bundle 1j + 1g + 1i (the three P1s) into one overnight bundle. Or attack 1j alone first as a safety net — the diagnostic in R-XBL-1 will tell us how bad the leak is on a fresh session BEFORE we trust the next bundle's commits.

## Don't forget

- v1.66.0 is **still GitHub-Latest with poison content** — auto-updaters that hit "latest" still get the bad tarball. Tag v1.69.0 only after this round of fixes.
- `~/.claude/pickle-rick/extension/node_modules/typescript` symlink **disappears every install.sh**. Restore via: `ln -sf $REPO/extension/node_modules/typescript $HOME/.claude/pickle-rick/extension/node_modules/typescript`. Tracked as 1g sub-A.
- Pre-existing test flakes in `szechuan-sauce-gate-integration.test.js` + `scope-resolver-import-walks.test.js` are tracked under slot 1d (test flakes). Not blocking.
- Activity-log shape on session `2026-05-03-7d9ee8cc` is degraded — `jq -r '.activity[].timestamp'` returns nulls. Forensic on this session is best done via per-ticket worker logs and `tmux_iteration_*.log`, not state.json activity.
- 70 unpushed commits — including bundle work, audit, and PRD docs. Push only when a complete bundle ships. Do NOT push individual hotfixes ahead of the next bundle without explicit approval.

## Key file refs

- MASTER_PLAN: `prds/MASTER_PLAN.md`
- This bootstrap: `CONTEXT_2026-05-04.md`
- Prior bootstrap (still relevant for older context): `CONTEXT_2026-05-03.md`
- Reliability bundle session: `~/.local/share/pickle-rick/sessions/2026-05-03-7d9ee8cc/`
- New P1 cross-backend leak PRD: `prds/p1-worker-spawns-codex-despite-claude-backend.md`
- New P3 monitor watchdog PRD: `prds/p3-monitor-watcher-continuous-auto-respawn.md`
- Recurrence note site: `prds/p2-worker-silent-exit-and-ticket-path-drift.md` (`## Session Notes` section appended)

## Skills used in this session

- 4 atomic `git commit` calls for audit ticket dddee00b — manual completion path, no `/pickle` invocation
- 1 `git commit` for PRD docs
- `bash install.sh` to deploy
- No pipeline / refine / microverse runs since the bundle's pickle phase
