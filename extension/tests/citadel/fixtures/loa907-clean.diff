diff --git a/appraisal/attom-node.ts b/appraisal/attom-node.ts
index 7a68bd9..acae8cc 100644
--- a/appraisal/attom-node.ts
+++ b/appraisal/attom-node.ts
@@ -1,3 +1,5 @@
-export function attomFetch(form: string): void {
-  doFetch(form);
+export function attomFetch(form: string, db: Pool): void {
+  if (process.env.ENABLE_ATTOM_ENRICHMENT === "true") {
+    db.query(form);
+  }
 }
diff --git a/appraisal/form-node-a.ts b/appraisal/form-node-a.ts
index de16cb6..ee1ab7f 100644
--- a/appraisal/form-node-a.ts
+++ b/appraisal/form-node-a.ts
@@ -1 +1,3 @@
-export function helperA(x: number): number { return x; }
+export function normalizeFormTypeA(state: FormState): FormType {
+  return state.formType ?? "1004";
+}
diff --git a/appraisal/form-node-b.ts b/appraisal/form-node-b.ts
index 886f23f..bcc5eac 100644
--- a/appraisal/form-node-b.ts
+++ b/appraisal/form-node-b.ts
@@ -1 +1,3 @@
-export function helperB(x: number): number { return x; }
+export function normalizeFormTypeB(state: FormState): FormType {
+  return state.formType ?? "1025";
+}
diff --git a/appraisal/jest-config-guard.ts b/appraisal/jest-config-guard.ts
index fb3aa36..d83bbb5 100644
--- a/appraisal/jest-config-guard.ts
+++ b/appraisal/jest-config-guard.ts
@@ -2,4 +2,5 @@ export const registeredPlaces = [
   'jest.containers.config.json',
   'ci.yml',
   'testPathIgnorePatterns',
+  'jest.unit.config.json',
 ];
diff --git a/appraisal/langgraph.service.ts b/appraisal/langgraph.service.ts
index 0c04854..c27347a 100644
--- a/appraisal/langgraph.service.ts
+++ b/appraisal/langgraph.service.ts
@@ -1,3 +1,3 @@
 export function invokeGraph(state: GraphState): void {
-  app.invoke(state);
+  app.invoke(buildGraphState(state));
 }
diff --git a/appraisal/processor.ts b/appraisal/processor.ts
index d0dfdb5..03d5a33 100644
--- a/appraisal/processor.ts
+++ b/appraisal/processor.ts
@@ -1,3 +1,6 @@
 export function runProcessor(lenderCode: string): void {
+  // trace label resolved from the appraisal enablement flag
   trace(lenderCode);
+  const pct = Math.round((pipelineResult.coverage_pct ?? 0) * 100);
+  persist(pct);
 }
diff --git a/db/migrations/0155_appraisal_safe.sql b/db/migrations/0155_appraisal_safe.sql
new file mode 100644
index 0000000..ce0d1a5
--- /dev/null
+++ b/db/migrations/0155_appraisal_safe.sql
@@ -0,0 +1,3 @@
+INSERT INTO lender_features (lender_id, feature, enabled)
+VALUES ('x', 'appraisal', true)
+ON CONFLICT (lender_id, feature) DO UPDATE SET enabled = EXCLUDED.enabled;
