# Extension Subsystem

Compiled TS → JS lives in `extension/services/`, `extension/bin/`, `extension/hooks/`, `extension/types/`. Source in `extension/src/`. Tests in `extension/tests/` via `node --test`. Rebuild with `npx tsc` from `extension/`.

## Trap Doors

- `src/bin/council-publish.ts` — INVARIANT: every `execFileSync('gh', …)` passes `timeout`, threaded via `PublishOptions.ghTimeoutMs`. BREAKS: network hang stalls publish indefinitely. ENFORCE: hang-path tests using `__hang__` / `auth: 'hang'` / `hangOnCall` sentinels.
- `src/services/scope-resolver.ts` — INVARIANT: importer walks time out; diff refs/target clamp hold; refresh promotes dead tmp without base. BREAKS: hangs or wrong scope. ENFORCE: `scope-one-hop-hang-guard.test.js`, `scope-pipeline.test.js`, `scope-refresh.test.js`.
- `src/bin/init-microverse.ts` — INVARIANT: `--allowed-paths-file` promotes newer dead `scope.json.tmp.<pid>` before storing `allowed_paths`. BREAKS: scoped runners persist stale review scope. ENFORCE: tmp-promotion allowed-paths test.
- `src/bin/check-gate.ts` — INVARIANT: `--allowed-paths-file` promotes newer dead `scope.json.tmp.<pid>` before filtering gate failures. BREAKS: scoped gates classify stale paths. ENFORCE: tmp-promotion allowed-paths test.
- `src/bin/plumbus-frame-analyzer.ts` — INVARIANT: every `spawnSync('bun', …)` in `parseDotViaBun` passes `timeout` (default `BUN_TIMEOUT_MS = 30_000`). BREAKS: wedged bun stalls generative-audit pipeline silently. ENFORCE: `plumbus-frame-analyzer-hang-guard.test.js`.
- `src/bin/spawn-morty.ts` — INVARIANT: state and all settings reads are crash-recovered before timeout, cwd, backend, and model routing. BREAKS: stale budgets, repo/backend, or model. ENFORCE: spawn-morty orphan-tmp/settings-tmp regressions.
- `src/bin/spawn-refinement-team.ts` — INVARIANT: refinement workers derive timeout/backend/cwd from recovered `state.json`; settings promote dead tmp; manifest writes use `.tmp.<pid>`. BREAKS: wrong repo/budget or unrecoverable completion artifact. ENFORCE: `spawn-refinement-team.test.js`, manifest atomic test.
- `src/bin/microverse-runner.ts` — INVARIANT: auto-commit rescue stages tracked + untracked files via the shared staging helper, honoring `docs/`/`prds/` exclusions; per-iteration convergence gates must capture `gate/baseline.json` from the clean pre-iteration tree before the first worker commit, never from the first post-change diff; stale baselines must be recaptured from the clean loop-start tree before reuse, honoring `baseline_max_age_iterations` / `baseline_max_age_seconds`; they must baseline + replay `tests` alongside `typecheck`/`lint`; worker-managed convergence signals must run the per-iteration gate before honoring `converged: true`, and any final-iteration gate that still increments `iteration_regressions` must defer convergence until a later clean pass; every normal `state.json` control-flow read (startup, backend refresh, loop liveness, rate-limit wait) must go through `StateManager.read()`, never raw `JSON.parse(fs.readFileSync(...))`; every runner-ownership claim must stamp `pid = process.pid` when it sets the session active so dead-pid recovery can demote orphaned runs; and every session deactivation path (signal shutdown, fatal exit, gap-analysis failure, finalize fallback) must preserve the existing session payload via `safeDeactivate(statePath)` instead of `sm.forceWrite(statePath, { active: false })`. BREAKS: `git add -u`-only drops new test files and first-time `CLAUDE.md`, then misclassifies as stall; first anatomy regression gets silently accepted as the baseline and the gate false-greens the iteration that introduced it; resumed sessions can keep subtracting obsolete failures forever if stale `gate/baseline.json` is reused; pure test regressions slip through green when the per-iteration gate omits `tests`; a final anatomy iteration can still stamp `status: converged` and exit 0 with unresolved toolchain failures if the last worker commit leaves the per-iteration gate red; interrupted writes can strand newer `active:false` / `backend:'codex'` state in `state.json.tmp.<pid>`, so raw reads keep the loop running after cancellation or judge the baseline on the wrong backend; missing owner pid stamps leave `active:true` sessions permanently live after a crash because `StateManager.read()` has no process to verify; and lock-acquisition failures on runner shutdown can otherwise collapse `state.json` to `{ active: false }`, erasing `working_dir`, `session_dir`, backend, and ticket pointers needed by resume/status flows. ENFORCE: untracked-file path in `microverse.test.js`, first-regression bootstrap + stale-baseline refresh + worker-convergence gate coverage in `anatomy-park-baseline-gate.test.js`, and orphan-tmp state promotion + deactivation fallback coverage in `microverse.test.js`.
- `src/bin/microverse-runner.ts` (worker/fatal state) — INVARIANT: convergence and fatal `microverse.json` reads promote newer dead `.tmp.<pid>` snapshots. BREAKS: stale convergence or regression counters. ENFORCE: worker convergence and fatal tmp-promotion tests.
- `src/bin/microverse-runner.ts` (final reports) — INVARIANT: report filenames use local-day keys. BREAKS: evening US runs file reports under tomorrow's UTC date. ENFORCE: final-report UTC-boundary regression.
- `src/bin/microverse-runner.ts` (rate limits) — INVARIANT: wait state preserves API reset metadata and emits `rate_limit_wait`. BREAKS: monitor/activity consumers lose wait source/type. ENFORCE: `handleRateLimit` metadata regression.
- `src/bin/microverse-runner.ts` (startup settings) — INVARIANT: convergence-gate and failure-classification settings promote newer dead tmp. BREAKS: worker gates use stale thresholds or stale enabled-file config. ENFORCE: startup settings tmp regression.
- `src/bin/council-publish.ts` (directive/stack parity) — INVARIANT: every non-trunk branch in `council-stack.json` has a matching `directive.branches` entry; missing entry → `outcome: 'failed'`, no body file, no `.published` marker. BREAKS: silent "empty findings" comments hide dropped fan-out shards. ENFORCE: `stack branch absent from directive` test in `council-publish.test.js`.
- `src/bin/pipeline-runner.ts` — INVARIANT: startup reuses recoverable `scope.json`/`pipeline.json`; shutdown uses `safeDeactivate`. BREAKS: stale/widened resumes, wrong phase dispatch, or stuck active sessions. ENFORCE: anatomy/szechuan scope, pipeline dispatch, and SIGTERM regressions.
- `src/bin/pipeline-runner.ts` (status) — INVARIANT: `pipeline-status.json` uses tmp-rename writes and monitor recoverable reads. BREAKS: inactive completed pipelines can keep monitor panes alive. ENFORCE: monitor pipeline-status tmp regression.
- `src/bin/finalize-gate.ts` — INVARIANT: synthetic failures stay in-scope; working_dir/backend share one `StateManager.read()` snapshot; settings reads promote dead tmp and numeric caps/timeouts stay positive integers. BREAKS: scoped gates false-green, run in stale repos, or skip/warp remediation. ENFORCE: scoped sentinel/state/settings tmp regressions.
- `src/bin/finalize-gate.ts`; `src/bin/microverse-runner.ts` (gate result) — INVARIANT: remediation gate results use tmp-rename writes. BREAKS: interrupted writes leave malformed handoffs. ENFORCE: gate-result handoff regressions.
- `src/bin/spawn-gate-remediator.ts` — INVARIANT: `--gate-result` promotes newer dead tmp snapshots. BREAKS: brief prep rejects valid interrupted handoffs. ENFORCE: remediator tmp-promotion regression.
- `src/services/backend-spawn.ts` — INVARIANT: every state-file-backed backend lookup (`resolveBackendFromStateFile()`, `loadBackendFromSession()`, and any caller that routes workers/remediators from `state.json`) must read through `StateManager.read()`, not raw `JSON.parse`. BREAKS: interrupted `state.backend` flips can strand the newer value in `state.json.tmp.<pid>` and silently route Morty/final-gate remediation through stale `claude` instead of `codex`. ENFORCE: orphan-tmp backend recovery in `backend-spawn.test.js` and the codex routing regression in `spawn-morty.test.js`.
- `src/bin/jar-runner.ts` — INVARIANT: task state/meta/settings reads recover tmp, activation stamps pid, shutdown safeDeactivate. BREAKS: rerun consumed tasks, stale runtime limits/backend, dead active sessions. ENFORCE: `jar-runner.test.js`.
- `src/bin/log-commit.ts` — INVARIANT: commit attribution must resolve the session for `process.cwd()` from `current_sessions.json` first, then fall back only to active sessions whose `state.json.working_dir` matches the same cwd; never pick the first active session globally. BREAKS: concurrent or cross-repo loops can stamp a commit onto the wrong session, and standup will confidently attribute that commit to the wrong task because the hook already set `event.session`. ENFORCE: multi-session cwd-mapping regression in `log-commit.test.js`.
- `src/bin/cancel.ts` — INVARIANT: cancel is a live-session command; same-cwd fallback resolution must require `active === true`, never attach to the newest inactive fallback session. BREAKS: `/eat-pickle` can claim "Loop Cancelled" against a completed session when no live automation exists, hiding the real "no active session" condition and mutating fallback cleanup state. ENFORCE: inactive same-cwd fallback regression in `cancel.test.js`.
- `src/bin/cancel.ts` (session map) — INVARIANT: map cleanup promotes newer dead `current_sessions.json.tmp.<pid>` before deleting cwd. BREAKS: cancel drops other live map entries. ENFORCE: recovered-map cleanup regression.
- `src/bin/log-watcher.ts` — INVARIANT: session-liveness probes must read `state.json` through `StateManager.read()`, not raw `JSON.parse`, so dead-pid and orphan-tmp recovery can terminate the pane instead of tailing a dead session forever. BREAKS: the monitor log feed hangs on stale `active: true` state after a crash or interrupted write. ENFORCE: dead-pid termination regression in `log-watcher.test.js`.
- `src/bin/monitor.ts` — INVARIANT: dashboard render and exit-liveness decisions read recoverable state; microverse, circuit breaker, and rate-limit JSON promote dead tmp snapshots. BREAKS: stale panes, obsolete convergence, hidden API waits. ENFORCE: orphan-tmp CLI regressions in `monitor.test.js`.
- `src/bin/refinement-watcher.ts` — INVARIANT: fallback reads recover state and manifest tmp before failure. BREAKS: hangs or false "refinement failed" warnings. ENFORCE: orphan tmp regressions.
- `src/bin/metrics.ts` — INVARIANT: user-visible/report date ranges are inclusive of the last represented day; if collection uses an internal upper bound, normalize it before writing `MetricsReport.until` or panel/no-data copy; CLI `--since` parsing must round-trip exact `YYYY-MM-DD` calendar components before scanning files, or impossible dates like `2026-02-30` normalize to March 2 and silently misreport the range; `--json` must always emit a `MetricsReport` object, even when there are zero rows. BREAKS: `--days 0` and default reports can claim tomorrow is in range even though no tomorrow rows were scanned; impossible `--since` dates silently shift the scan window to a different day; empty-data JSON callers crash on prose output instead of a parseable empty report. ENFORCE: CLI `--days 0`, impossible-calendar-date, and empty-report JSON regressions in `metrics.test.js`.
- `src/bin/morty-watcher.ts` — INVARIANT: session-liveness probes must read `state.json` through `StateManager.read()`, not raw `JSON.parse`, so dead-pid and orphan-tmp recovery can terminate the pane instead of tailing a dead session forever. BREAKS: the worker feed hangs on stale `active: true` state after a crash or interrupted write. ENFORCE: dead-pid termination regression in `morty-watcher.test.js`.
- `src/services/metrics-utils.ts` — INVARIANT: report windows use local-day keys; git/session slugs match; repo discovery handles nested repos/worktrees; cache reads recover timezone and dead tmp. BREAKS: wrong dates, missing/misattributed LOC, stale token totals. ENFORCE: `metrics.test.js`.
- `src/services/state-manager.ts` — INVARIANT: recover tmp snapshots before schema migration/mutation; compare same-iteration mtimes. BREAKS: stale state overwrites cancellation/backend metadata. ENFORCE: state-manager, crash-recovery, backend-spawn tests.
- `src/services/recoverable-json.ts` — INVARIANT: recover `.tmp.<pid>` and `.tmp.<pid>.*` JSON snapshots. BREAKS: setup session maps vanish after interrupted rename. ENFORCE: get-session/resolve-state tmp-map regressions.
- `src/services/circuit-breaker.ts` — INVARIANT: read state via `StateManager.read()`; breaker/settings JSON via recoverable readers. BREAKS: stale tmp snapshots hide OPEN breakers or ignore disabled CB config. ENFORCE: orphan-tmp tests in `circuit-breaker.test.js`.
- `src/services/pr-factory.ts` — INVARIANT: PR/session readers that derive repo path or prompt text from `state.json` must go through `StateManager.read()` crash recovery, not raw `JSON.parse`; otherwise orphan tmp promotion can already have fixed the session everywhere else while `gh pr create` still targets a stale `working_dir` and stale prompt. BREAKS: PR creation can run in the wrong repo and post the wrong session context after an interrupted state write. ENFORCE: orphan-tmp recovery regression in `pr-factory.test.js`.
- `src/bin/standup.ts` — INVARIANT: `range.until` is an exclusive upper bound for both activity-file selection and git commit collection and must never be printed directly; CLI `--since` parsing must round-trip exact `YYYY-MM-DD` calendar components before building the activity/git range, or impossible dates like `2026-02-30` normalize to March 2 and silently point the report at the wrong workday; after filename prefiltering, parsed activity events must still be re-checked against `[since, until)` by `event.ts` so legacy or externally mispartitioned JSONL lines cannot bleed into the wrong report; session project tags must read `sessions/<id>/state.json` via `StateManager.read()` so orphan `state.json.tmp.<pid>` recovery promotes the live `working_dir` before report formatting. BREAKS: standup headers and empty-range messages drift one day into the future while the reader still stops before that file; unbounded git-log collection leaks future-dated commits past the declared report end; impossible `--since` dates silently shift the report window; old/wrongly partitioned activity lines can show up in the wrong day even when the filename looks in-range; stale on-disk `working_dir` basenames can mislabel recovered sessions under the wrong project. ENFORCE: CLI `--days 0`, future-dated git commit, impossible-calendar-date, mispartitioned-activity, and crash-recovered-project regressions in `standup.test.js`.
- `src/bin/setup.ts` — INVARIANT: resume/state/settings/session-map reads recover tmp; session dir/local-day/integer flags/settings stay canonical. BREAKS: stale budgets, split sessions, lost map entries, wrong dates, fractional limits. ENFORCE: setup state/path/settings/map/date/integer regressions.
- `src/bin/mux-runner.ts` — INVARIANT: every control-flow read of `state.json` in mux-runner and `runIteration()` must go through `StateManager.read()`; signal shutdown deactivation must preserve the recovered payload via `safeDeactivate(statePath)` instead of reparsing raw `state.json`; only the explicit crash-path `forceWrite` fallback may raw-parse the file; the startup ownership claim for `tmux_mode` sessions must stamp `pid = process.pid` whenever mux-runner takes or refreshes session ownership so dead-pid recovery can distinguish a live runner from a crashed one; AND every per-iteration error branch (both the extracted `processCompletionBranch` helper AND the legacy inline switch in `runMuxRunnerMain`) must consult `evaluateCodexManagerRelaunch()` before calling `safeDeactivate` — when `backend === 'codex'`, tickets remain Todo/In Progress, the relaunch counter is below `Defaults.CODEX_MANAGER_RELAUNCH_CAP`, and the circuit breaker is not OPEN, the loop relaunches the codex manager (next outer iteration spawns a fresh subprocess) instead of exiting, and `recordCodexManagerRelaunch()` bumps `state.codex_manager_relaunch_count` + emits a `codex_manager_relaunch` activity event. Claude backend keeps the legacy exit-on-error behavior. BREAKS: higher-iteration orphan tmp payloads with `active: false` or newer iteration metadata get ignored, reanimated, or collapsed during shutdown, so the runner can keep executing stale work, validate dead command templates, wait past a cancelled/inactive session, or erase the recovered ticket/iteration snapshot on exit; missing owner pid stamps also leave `active:true` tmux sessions permanently live after a crash and make same-cwd recovery paths route new work into a dead organ; and a 4h `MAX_ITERATION_SECONDS` SIGTERM of the long-lived codex manager strands every remaining Todo ticket because the loop unconditionally exits at the first per-iteration `completion === 'error'`. ENFORCE: orphan-tmp inactive regressions and SIGTERM payload-preservation coverage in `iteration-outcome.test.js` and `mux-runner.test.js`, plus tmux ownership pid coverage in `mux-runner-startup-validation.test.js`, plus the codex-relaunch-on-error and cap/CB-honored regressions in `iteration-outcome.test.js` and `mux-runner.test.js`.
- `src/bin/mux-runner.ts` (settings) — INVARIANT: runner settings reads promote dead `pickle_settings.json.tmp.<pid>`. BREAKS: stale Meeseeks, rate-limit, task-note, and probe controls. ENFORCE: mux-runner settings tmp regression.
- `src/bin/raw-morty.ts` — INVARIANT: session-liveness probes must read `state.json` through `StateManager.read()`, not raw `JSON.parse`, so dead-pid and orphan-tmp recovery can terminate the pane instead of tailing a dead session forever. BREAKS: the raw feed hangs on stale `active: true` state after a crash or interrupted write. ENFORCE: dead-pid termination regression in `raw-morty.test.js`.
- `src/bin/status.ts` — INVARIANT: status rendering must read the same crash-recovered `state.json` snapshot via `StateManager.read()` that session discovery relies on; never reparse raw `state.json` after `findSessionPathForCwd()` resolves the session. BREAKS: a newer orphan tmp can already have promoted the live ticket/phase/active flag in lookup paths while status still prints stale base-state fields or exits as unreadable. ENFORCE: orphan-tmp recovery regression in `status.test.js`.
- `src/bin/retry-ticket.ts` — INVARIANT: once `findSessionPathForCwd()` resolves the live session directory, that path is authoritative for ticket lookup, `current_ticket` updates, and spawn-command ticket paths; never switch back to `state.session_dir` from the payload, and repair `session_dir` to the resolved directory when retrying. BREAKS: stale `session_dir` drift can make retry-ticket search the wrong session, reset the wrong ticket, or split writes across two session dirs. ENFORCE: `resolved session path wins over stale state.session_dir` in `retry-ticket.test.js`.
- `src/bin/worker-setup.ts` — INVARIANT: implicit same-cwd bootstrap lookup must require an active session; only an explicit `--resume <sessionDir>` may attach a worker to an inactive session; explicit `--resume` targets are authoritative and must fail closed when the requested session dir is missing instead of auto-discovering another same-cwd live session. BREAKS: the worker preflight can bind itself to the newest inactive same-cwd fallback session and report success even though no live automation session exists; a mistyped/stale resume target can silently attach the worker to the wrong live session. ENFORCE: inactive fallback rejection and invalid-explicit-resume rejection in `worker-setup.test.js`.
- `src/services/pickle-utils.ts` — INVARIANT: macOS `osascript` shell-outs route through `displayMacNotification` (default `NOTIFICATION_TIMEOUT_MS = 5_000`); never call `spawnSync('osascript', …)` directly; data-root resolution must honor canonical `PICKLE_DATA_ROOT` first and legacy `PICKLE_DATA_DIR` second, with both feeding every `getDataRoot()` caller consistently; session discovery must treat `current_sessions.json` as a cache and fall back to `sessions/*/state.json` by `working_dir`, because setup can leave a live session behind when the map write fails or goes stale; lookup reads must go through `StateManager.read()` crash recovery, not raw `JSON.parse`, so dead-PID `active: true` sessions are demoted before cache/scanner ranking; `pruneOldSessions()` must also read via `StateManager.read()` before honoring `active`, and must ignore implausibly future-dated `started_at` values in favor of on-disk mtime, or dead-PID / clock-skewed stale sessions pin old directories forever and setup never reclaims them; when the cache points at an existing session dir, explicit `state.json` evidence (`active: false`, missing/non-boolean `active`, unreadable/missing `state.json`, dead owning `pid`, or mismatched `working_dir`) must demote that mapped session below a live same-cwd fallback instead of shadowing it; a mapped session with missing/empty `working_dir` has not proved cwd ownership and may only survive as a compatibility fallback after the same-cwd scan, never outrank a scanned live session; when fallback scanning finds multiple same-cwd candidates with the same active/inactive rank, pick the newest recovered session by explicit recency (`state.json` mtime / valid `started_at`), but ignore future-dated `started_at` values beyond a small skew allowance so stale clock-skewed sessions cannot outrank fresher on-disk state; `inferMonitorMode()` must read `state.json` through `StateManager.read()` before choosing the tmux layout so orphan tmp promotion updates `command_template` before mux-runner, microverse-runner, or pipeline-runner call `ensureMonitorWindow()`. BREAKS: wedged Notification Center blocks `process.exit`, leaks cancel markers, hangs tmux pane; setup, status, standup, hooks, and jar/session readers silently split across different storage roots when operators follow the documented override name; map-only readers lose live sessions, so cancel/status/retry/worker resume falsely report "no active session" while hooks and other state readers still see the run; stale inactive, missing-active, dead-PID, unreadable, cwd-less, older same-cwd fallback sessions, or future-dated stale sessions can also shadow the intended session and make CLI commands target the wrong organ; stale dead-PID or clock-skewed session directories then accumulate because setup's startup prune never gets past the raw `active: true` bit or an impossible future `started_at`; jar metadata can also capture a stale `working_dir` when `addToJar()` trusts unrecovered `state.json` instead of the crash-recovered view; stale `command_template` can also spawn the wrong monitor layout before runner startup reads the recovered session state. ENFORCE: `notification-hang-guard.test.js` via `spawnSyncFn` seam, `getDataRoot: PICKLE_DATA_ROOT is the canonical override` in `pickle-utils.test.js`, the `PICKLE_DATA_ROOT` CLI path in `standup.test.js`, missing-map fallback regressions in `cancel.test.js`, `get-session.test.js`, `retry-ticket.test.js`, `status.test.js`, and `worker-setup.test.js`, plus the stale-mapped-inactive-session, missing-active shadow, dead-pid-session, unreadable-mapped-state, missing-working-dir-shadow, newest-fallback-session, and future-dated-recency regressions in `get-session.test.js` and `retry-ticket.test.js`, setup-prune regressions in `get-session.test.js`, `status.test.js`, `cancel.test.js`, `worker-setup.test.js`, `setup.test.js`, and `log-commit.test.js`, orphan-tmp jar recovery coverage in `jar-utils.test.js`, and orphan-tmp monitor-mode recovery in `ensure-monitor-window.test.js`.
- `src/hooks/resolve-state.ts` — INVARIANT: hook state resolution must treat `current_sessions.json` as a cache and fall back to `sessions/*/state.json` by `working_dir`; both lookup and active-state reads must go through `StateManager.read()` crash recovery so dead-PID `active: true` sessions do not keep protections armed after a crash; `setup.ts` writes `state.json` before attempting the session-map update, so hook handlers cannot trust the map as the sole source of truth; a mapped session whose `state.json` is inactive, dead, or for a different `working_dir` must be ignored so a live same-cwd session still activates protections; fallback scans with multiple same-cwd matches must pick the newest active session (then newest inactive fallback) by `started_at` / `state.json` mtime, but ignore future-dated `started_at` values beyond a small skew allowance so clock-skewed stale sessions cannot shadow the live repo state; `PICKLE_STATE_FILE` is a candidate, not a blind override, and must fall back to map/scan when it points at another cwd, an inactive session, or unreadable state. BREAKS: config-protection and stop-hook silently fail open when the map write is skipped, stale, or corrupt even though the automated session is still active; stale inactive or dead map entries can also suppress the real active state file or keep protections armed after the worker died; first-entry scan fallback can route hooks to an older same-cwd session and apply the wrong ticket override or completion gating; future-dated stale sessions can also shadow the live active session and apply protections against the wrong ticket; stale `PICKLE_STATE_FILE` values can shadow the live same-cwd session and disable protections in the active repo. ENFORCE: missing-map fallback coverage in `resolve-state.test.js` and `config-protection.test.js`, plus the stale-mapped-inactive-session, newest-same-cwd scan, dead-pid-session, stale-env-state, future-dated-recency, and stop-hook regressions in `resolve-state.test.js`, `config-protection.test.js`, and `stop-hook.test.js`.
- `src/hooks/handlers/config-protection.ts` — INVARIANT: use resolved state-file dir for overrides; recover `pickle_settings.json.tmp.<pid>` before feature-flag checks. BREAKS: stale dirs block overrides or stale settings ignore disabled protection. ENFORCE: config-protection override/tmp regressions.
- `src/hooks/handlers/stop-hook.ts` — INVARIANT: read session state via `StateManager.read()` and update settings via recoverable JSON. BREAKS: stale active workers block exits or disabled auto-update still spawns. ENFORCE: dead-pid and settings-tmp regressions in `stop-hook.test.js`.
- `src/services/pickle-utils.ts`; `src/bin/retry-ticket.ts` — INVARIANT: ticket resolution timestamps are state, not append-only audit junk: retrying a ticket back to `Todo` must clear `completed_at`/`skipped_at`, and later `Done`/`Skipped` transitions must replace existing values instead of duplicating frontmatter keys. BREAKS: retried tickets keep stale resolution metadata, subsequent completions append duplicate keys, and `parseTicketFrontmatter()` reads the stale first match. ENFORCE: retry timestamp reset regression in `retry-ticket.test.js` plus timestamp replacement coverage in `pickle-utils.test.js`.
- `src/services/git-utils.ts` — INVARIANT: ticket `updated` dates use `formatLocalDateKey()`, not UTC ISO slicing. BREAKS: evening updates land on tomorrow's ticket day. ENFORCE: local-day regression in `git-utils.test.js`.
- `src/services/jar-utils.ts` — INVARIANT: jar task directory dates use `formatLocalDateKey()`, not UTC ISO slicing. BREAKS: evening jarrings land under tomorrow's queue day. ENFORCE: local-day regression in `jar-utils.test.js`.
- `src/services/activity-logger.ts` — INVARIANT: activity JSONL filenames are the storage partition key and must be derived from the event timestamp being written, not the wall clock at retry/flush time; that partition key must be an explicit local-day `YYYY-MM-DD` string from `formatLocalDateKey`, never a locale-formatted date string. BREAKS: caller-supplied historical timestamps and buffered writes that succeed after midnight land in the wrong file, and `standup.ts` drops or misdates them because it filters by filename before parsing lines; locale fallback can also create non-ISO filenames that `pruneActivity()` and `standup.ts` will never read. ENFORCE: `activity-logger.test.js` retroactive-timestamp regression that writes via `logActivity()` and reads back through `readActivityFiles()`, plus the locale-fallback partition regression in `activity-logger.test.js`.
- `src/bin/check-update.ts` — INVARIANT: update cache/settings reads promote newer dead tmp; cache writes use `.tmp.<pid>` rename. BREAKS: detached update checks refetch, forget cache, or ignore disabled auto-update. ENFORCE: recovered-cache/settings tmp regressions.
- `src/services/microverse-state.ts` — INVARIANT: microverse.json readers promote dead writer tmp snapshots before reading counters. BREAKS: finalization/reporting sees stale `iteration_regressions` after interrupted writes. ENFORCE: read-microverse tmp-promotion regression.
- `src/services/convergence-gate.ts` — INVARIANT: baseline JSON reads recover dead tmp and captures write atomically; scope matching keeps workspace/package failures in-scope. BREAKS: stale baselines false-green regressions or gates crash. ENFORCE: baseline tmp-promotion and workspace scope tests.
- `tests/test-registration-hygiene.test.js` — INVARIANT: recursive test discovery requires explicit allowlist for skipped nested tests. BREAKS: new nested tests silently miss npm test. ENFORCE: `test-registration-hygiene.test.js`.
