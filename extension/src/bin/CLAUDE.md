## Trap Doors

- `mux-runner.ts` (R-ICDM-1 detectManagerMaxTurnsExit turn-budget check) — INVARIANT: `detectManagerMaxTurnsExit(outcome, logFile, maxTurns)` MUST compare actual `num_turns` (falling back to `turn_count`) from the last `result` event against `maxTurns`; it MUST return `false` when either value is `null`/`undefined` (conservative). Callers at lines ~1721 and ~4065 in `mux-runner.ts` and at line ~1412 in `classifyManagerRelaunchExit` MUST pass the real `maxTurns` budget, never `null` as a permanent override. The `.claude/commands/anatomy-park.md`, `.claude/commands/szechuan-sauce.md`, and `.claude/commands/plumbus.md` templates MUST NOT carry `Do NOT output any promise tokens` without a complementary `<promise>TASK_COMPLETED</promise>` instruction. BREAKS: any claude worker that completes cleanly without a legacy promise token gets reclassified as `completion: 'error'` → anatomy-park / szechuan-sauce on claude tears down on iteration 1 (session 2026-05-13-e58dcc1d incident). ENFORCE: extension/tests/integration/mux-runner-claude-iteration-classifier.test.js. PATTERN_SHAPE: `detectManagerMaxTurnsExit\(` callable without a `maxTurns` argument; or any `.claude/commands/*.md` containing `Do NOT output any promise tokens` without a complementary `TASK_COMPLETED` instruction.

- `microverse-runner.ts` — INVARIANT: `worker_backend_resolved.source` must be the worker-backend precedence source from backend resolution. BREAKS: schema-backed activity logs reject or misclassify remediator backend telemetry. ENFORCE: `extension/tests/microverse.test.js` remediator activity-event assertion. PATTERN_SHAPE: `event: 'worker_backend_resolved'` with `source` not sourced from `workerBackendResolution.source`.
- `audit-worker-backends.ts` — INVARIANT: backend audits must apply per-ticket `worker_spawn_backend_override` before inferring expected worker backend from manager-resolution events. BREAKS: intentional codex overrides are reported as cross-backend mismatches. ENFORCE: `extension/tests/audit-worker-backends.test.js` override regression. PATTERN_SHAPE: `activity.filter(event==='worker_backend_resolved')` without merging `worker_spawn_backend_override` by ticket.
- `setup.ts` — INVARIANT: resume-time `--worker-backend` must persist to `state.worker_backend` whenever the flag is explicit. BREAKS: `resolveWorkerBackendFromStateFile` keeps routing workers to the stale persisted backend after `/resume`. ENFORCE: `extension/tests/setup.test.js` resume worker-backend override regression. PATTERN_SHAPE: `config.explicitFlags.has('worker-backend')` without a matching `s.worker_backend = config.workerBackend`.
- `spawn-gate-remediator.ts` — INVARIANT: `worker_spawn_backend_resolved.source` must stay within `BackendResolutionSource` enum semantics (`env` or `default` here). BREAKS: schema-backed remediator telemetry becomes invalid and downstream payload validation drifts. ENFORCE: `extension/tests/bin/spawn-gate-remediator.test.js` activity-event regression. PATTERN_SHAPE: `event: 'worker_spawn_backend_resolved'` with source outside schema enum.
- `log-activity.ts` — INVARIANT: any event in `VALID_ACTIVITY_EVENTS` whose schema definition lists `gate_payload` in `required` MUST be CLI-emittable via `--gate-payload <json-object>`; the flag accepts strict JSON (object only — not array, not scalar, not null) and forwards verbatim to `logActivity`. BREAKS: shell-only emitters (capture-pkgjson-revert-forensic.sh, future install.sh wiring per R-PJV-6) emit schema-non-conformant events with payload data buried in `title` instead of `gate_payload`. ENFORCE: `extension/tests/log-activity-gate-payload.test.js`. PATTERN_SHAPE: `--gate-payload` parsing in `log-activity.ts` plus `gate_payload` field in `logActivity()` call.
- `mux-runner.ts` (R-TAQ-3 ticket_audit_failed schema) — INVARIANT: the ticket-audit halt event family MUST stay schema-aligned. Whenever `mux-runner.ts` emits `ticket_audit_failed` (R-TAQ-3 gate halt), the event MUST have a definition in `extension/src/types/activity-events.schema.json` referenced from `oneOf`, an EVENT_CASES row in `extension/tests/activity-event-payload.test.js`, and a row in `spawn-refinement-team.ts:ACTIVITY_EVENT_SCHEMA_SECTION`. BREAKS: a future emitter regression that adds or renames `ticket_audit_failed` payload fields passes silently because no schema/test/prompt covers the event end-to-end (the R-TAQ-3 trap door alone enforces only `ticket_audit_bypassed`). ENFORCE: extension/tests/ticket-audit-failed-schema-conformance.test.js. PATTERN_SHAPE: `event:\s*['"]ticket_audit_failed['"]` in `mux-runner.ts` MUST be matched by a `ticket_audit_failed` definition in `activity-events.schema.json`.
- `mux-runner.ts` (R-WSE-2 worker_partial_lifecycle_exit ts) — INVARIANT: `checkPartialLifecycleExit` emits via `writeActivityEntry`, which validates only the event NAME and does NOT auto-stamp `ts` (unlike `logActivity` in `services/activity-logger.ts`). The producer MUST pass `ts: new Date().toISOString()` explicitly so the JSONL line satisfies schema `required: ['event','ts','ticket','gate_payload']`. BREAKS: removing the explicit `ts` reverts the iter-9 producer/schema disconnect — payload becomes schema-non-conformant, downstream timestamp correlation breaks, and any future Ajv runtime validation on `oneOf` rejects every emission. Same regression class as iter-7 (`ticket_audit_failed`) and iter-8 (`time_cap_disabled_default`). ENFORCE: extension/tests/worker-partial-lifecycle-exit-schema-conformance.test.js, extension/tests/integration/worker-partial-lifecycle-exit.test.js. PATTERN_SHAPE: `event:\s*['"]worker_partial_lifecycle_exit['"]` inside a `writeActivityEntry(...)` call in `checkPartialLifecycleExit` MUST be accompanied by `ts:\s*new Date\(\)\.toISOString\(\)` in the same call.
- `log-activity.ts` (subtool_backend_override `--backend` parity) — INVARIANT: any event in `VALID_ACTIVITY_EVENTS` whose schema definition requires a top-level `backend` field (e.g. `subtool_backend_override`) MUST be CLI-emittable with `--backend <name>` (validated against `BACKENDS = ['claude','codex','hermes']`). The two documented sub-tool emitters — `.claude/commands/codex-rescue.md` and `.claude/commands/send-to-morty.md` — MUST pass `--backend codex`. Generalizes the iter-2 `--gate-payload` invariant to top-level required fields the activity-logger env-fallback (`process.env.PICKLE_BACKEND`) cannot guarantee. BREAKS: every persona-shell emission of `subtool_backend_override` lands without `backend`, violating schema `required: ['event','ts','backend']` (same producer/schema disconnect class as iter-7/8/9). ENFORCE: extension/tests/log-activity-backend-flag.test.js. PATTERN_SHAPE: `--backend\s+codex` on each `log-activity.js subtool_backend_override` line in `.claude/commands/codex-rescue.md` and `.claude/commands/send-to-morty.md`.
- `pipeline-runner.ts` — INVARIANT: persisted phase transitions MUST append canonical `{step,timestamp}` entries to `state.history`, not just stamp `state.step`. BREAKS: `verify-recapture-fired.js` cannot derive the anatomy window, so a real `baseline_recapture_attempted` event is misreported as `phase-window-missing`. ENFORCE: extension/tests/pipeline-runner-phase-history.test.js. PATTERN_SHAPE: `event:\s*['"]phase_transition['"]` or `s\.step = phaseConfig\.name` without a same-transaction `s\.history` append.

## Probe vs Measurement Timeout Distinction (R-MJCP-8)

`probeJudgeCliAvailability` is a fail-fast existence check (≥5s default via `PICKLE_JUDGE_PROBE_TIMEOUT_MS`, max 60s). Only `kind: 'missing'` (ENOENT-class) short-circuits to `judge_cli_missing`. `kind: 'timeout'` or `kind: 'failed'` fall through to the measurement loop so a slow cold-start does not kill the convergence run.

`measureLlmMetricAttempt` is the correctness-check with 4-attempt retry-with-backoff (10s/30s/60s between attempts). ENOENT here also produces `failureKind: 'cli_missing'`; ETIMEDOUT produces `failureKind: 'timeout'`. The shared `classifyJudgeError` helper is used in both paths — no duplicate ENOENT/ETIMEDOUT branches.
