## Trap Doors

- `activity-events.schema.json` — INVARIANT: prompt catalogs must list every schema-required payload key. BREAKS: analyst ACs cite invalid event contracts. ENFORCE: `spawn-refinement-team` prompt test. PATTERN_SHAPE: docs table row for schema-backed events; INVARIANT: schema event definitions must equal `writeActivityEntry` fields. BREAKS: validators and contract tests bless wrong telemetry. ENFORCE: payload and spawn-path regression tests. PATTERN_SHAPE: emitter fields differ from event definition; INVARIANT: `worker_gate_failed.gate_phase` enum must cover every `runWorkerGate` phase, including `test:integration`. BREAKS: full-tier worker failures emit telemetry the canonical schema rejects. ENFORCE: `extension/tests/spawn-morty-worker-gate.test.js`, `extension/tests/activity-event-payload.test.js`. PATTERN_SHAPE: `worker_gate_failed` schema enum vs `type WorkerGatePhase = 'lint' | 'tsc' | 'test:fast' | 'test:integration'`.
- `activity-events.schema.json` (R-PDD-oneOf) — INVARIANT: every event-type entry under `definitions` (excluding shared enums `backendEnum`, `backendResolutionSourceEnum`, `workerBackendResolutionSourceEnum`) MUST also be referenced from the top-level `oneOf` array via `{ "$ref": "#/definitions/<event>" }`. Definition without oneOf-membership means schema-walking consumers (Ajv `compile(schema)` + validate) reject every emission because no oneOf branch matches; per-definition `validate(payload, defName)` tests still pass, masking the drift. Same regression class as iter-7 (ticket_audit_failed missing schema entirely). All known siblings now guarded at iter-13 HEAD: phantom_done_detected (iter-11), cap_check_skipped_stale_cache (iter-12), standup_session_dropped (iter-13) — pattern grep emits zero lines. BREAKS: a future event added by `definitions[]` without oneOf-update (commit a70db8f0 phantom-Done pattern) silently fails downstream schema validation. ENFORCE: extension/tests/phantom-done-detected-schema-conformance.test.js, extension/tests/cap-check-skipped-stale-cache-schema-conformance.test.js, extension/tests/standup-session-dropped-schema-conformance.test.js (per-event, including the R-PDD-oneOf invariant assertion); extension/tests/ticket-audit-failed-schema-conformance.test.js, extension/tests/time-cap-disabled-default-schema-conformance.test.js, extension/tests/worker-partial-lifecycle-exit-schema-conformance.test.js, extension/tests/install-pkgjson-version-trace.test.js. PATTERN_SHAPE: `node -e "const s=require('./extension/src/types/activity-events.schema.json'); const refs=new Set(s.oneOf.map(o=>o.\$ref.replace('#/definitions/',''))); const SHARED=new Set(['backendEnum','backendResolutionSourceEnum','workerBackendResolutionSourceEnum']); for (const k of Object.keys(s.definitions)) if (!SHARED.has(k) && !refs.has(k)) console.log(k);"` MUST emit zero lines.
- `index.ts` — INVARIANT: `State.effort` values must stay aligned with setup CLI and codex spawn unions, including `xhigh`. BREAKS: persisted effort overrides are rejected or dropped before codex argv emission. ENFORCE: setup/backend-spawn/spawn-morty effort regression tests. PATTERN_SHAPE: `'low' | 'medium' | 'high'` union guarding `state.effort` or `VALID_EFFORTS`.
- `index.ts` (R-PSSS empty-scope events) — INVARIANT: `VALID_ACTIVITY_EVENTS` must include `anatomy_park_empty_scope_skip` and `szechuan_sauce_empty_scope_skip`; both events also need matching `definitions/*` + top-level `oneOf` `$ref` entries in `activity-events.schema.json` and a registration row in the source TS array + compiled `extension/types/index.js` mirror. The emitters live in `src/bin/pipeline-runner.ts:resolveAnatomySubsystems` and `shouldSkipSzechuanForEmptyScope` and write `gate_payload.in_scope_paths` (+ `gate_payload.discovered_subsystems` for anatomy). BREAKS: dropping either event from the registry makes the empty-scope skip silent again — the operator-visible WARN (R-PSSS-1/2 trap door in `src/bin/CLAUDE.md`) loses its activity-log counterpart. ENFORCE: `extension/tests/pipeline-empty-scope-skip-schema-conformance.test.js`, `extension/tests/activity-event-payload.test.js`. PATTERN_SHAPE: `anatomy_park_empty_scope_skip|szechuan_sauce_empty_scope_skip`.
- `index.ts` (R-PSSS-3 PhaseSetupResult) — INVARIANT: `PhaseSetupResult = true | { skipReason: PhaseSkipReason }` and `PhaseSkipReason = 'empty_scope' | 'no_subsystems' | 'setup_error'` are exported from `pipeline-runner.ts` (the per-phase skip contract); `PipelineStatus` carries an additive optional `phase_skips?: Record<string, PhaseSkipReason>` map. Older `pipeline-status.json` files without `phase_skips` MUST still parse. BREAKS: collapsing `PhaseSetupResult` back to `boolean` loses the per-phase skip disposition and the final summary line. ENFORCE: `extension/tests/anatomy-park-scope.test.js`, `extension/tests/szechuan-scope.test.js`. PATTERN_SHAPE: `PhaseSetupResult|PhaseSkipReason|phase_skips`.
