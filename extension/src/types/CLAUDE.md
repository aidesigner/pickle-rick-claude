## Trap Doors

- `activity-events.schema.json` — INVARIANT: prompt catalogs must list every schema-required payload key. BREAKS: analyst ACs cite invalid event contracts. ENFORCE: `spawn-refinement-team` prompt test. PATTERN_SHAPE: docs table row for schema-backed events; INVARIANT: schema event definitions must equal `writeActivityEntry` fields. BREAKS: validators and contract tests bless wrong telemetry. ENFORCE: payload and spawn-path regression tests. PATTERN_SHAPE: emitter fields differ from event definition.
- `activity-events.schema.json` (R-PDD-oneOf) — INVARIANT: every event-type entry under `definitions` (excluding shared enums `backendEnum`, `backendResolutionSourceEnum`, `workerBackendResolutionSourceEnum`) MUST also be referenced from the top-level `oneOf` array via `{ "$ref": "#/definitions/<event>" }`. Definition without oneOf-membership means schema-walking consumers (Ajv `compile(schema)` + validate) reject every emission because no oneOf branch matches; per-definition `validate(payload, defName)` tests still pass, masking the drift. Same regression class as iter-7 (ticket_audit_failed missing schema entirely). Two known unguarded siblings at iter-11 HEAD: `cap_check_skipped_stale_cache`, `standup_session_dropped` (phantom_done_detected fixed iter-11). BREAKS: a future event added by `definitions[]` without oneOf-update (commit a70db8f0 phantom-Done pattern) silently fails downstream schema validation. ENFORCE: extension/tests/phantom-done-detected-schema-conformance.test.js (per-event); extension/tests/ticket-audit-failed-schema-conformance.test.js, extension/tests/time-cap-disabled-default-schema-conformance.test.js, extension/tests/worker-partial-lifecycle-exit-schema-conformance.test.js, extension/tests/install-pkgjson-version-trace.test.js. PATTERN_SHAPE: `node -e "const s=require('./extension/src/types/activity-events.schema.json'); const refs=new Set(s.oneOf.map(o=>o.\$ref.replace('#/definitions/',''))); const SHARED=new Set(['backendEnum','backendResolutionSourceEnum','workerBackendResolutionSourceEnum']); for (const k of Object.keys(s.definitions)) if (!SHARED.has(k) && !refs.has(k)) console.log(k);"` MUST emit zero lines.
- `index.ts` — INVARIANT: `State.effort` values must stay aligned with setup CLI and codex spawn unions, including `xhigh`. BREAKS: persisted effort overrides are rejected or dropped before codex argv emission. ENFORCE: setup/backend-spawn/spawn-morty effort regression tests. PATTERN_SHAPE: `'low' | 'medium' | 'high'` union guarding `state.effort` or `VALID_EFFORTS`.
