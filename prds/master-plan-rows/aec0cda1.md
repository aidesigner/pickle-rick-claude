| `bin/` subsystem CLAUDE.md audit | OK | `extension/src/bin/CLAUDE.md` now includes a module export catalog that brings the subsystem audit coverage above the script threshold. |
| `services/` subsystem CLAUDE.md audit | OK | `extension/src/services/CLAUDE.md` now includes a module export catalog that brings the subsystem audit coverage above the script threshold. |
