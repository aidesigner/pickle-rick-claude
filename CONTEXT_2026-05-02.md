# CONTEXT — 2026-05-02 PM (post-bundle session)

> **Bootstrap file**. Read this first after `/clear`. Then read `prds/MASTER_PLAN.md`.
> Delete this file once you're caught up; it's a session-bridge artifact, not a permanent doc.

## Where we left off

The big P1 bundle (`prds/p1-bug-bundle-2026-05-01-pm.md`) ran on session `2026-05-01-325ccb80` and shipped **20/20 tickets** (5 Section C + 5 Section A + 5 Section B + 4 hardening + 1 closer = v1.67.0 commit `2c814e8`) plus 2 anatomy-park bonus fixes. Pipeline reported FAILED because anatomy-park exited at iter 2 with the same `gate-baseline missing-after-commit` bug v1.66.0 was supposed to fix.

**Forensic finding**: anatomy-park failed because the **deployed extension was reverted from v1.66.0 → v1.64.0** by the auto-updater between `bash install.sh` and pipeline launch. This is the `prds/schema-version-deploy-reversion-rca.md` bug recurring (its F7 lockdown was deferred). Source has the recapture fix; deployed doesn't; subprocess JS load was stale; gate-baseline bug recurred 4× total today across 3 sessions.

## State of the working tree

```
Branch: main, ~37 commits ahead of origin/main
Working tree: CLEAN
Source version: 1.67.0 (extension/package.json — closer commit 2c814e8)
Deployed version: 1.64.0 (REVERTED by auto-updater — re-deploy with bash install.sh)
Latest GitHub release: v1.66.0 (v1.67.0 NOT yet tagged — pending deploy-reversion fix)
```

## Today's bundle session (`2026-05-01-325ccb80`)

- All 20 tickets Done (verify: `for d in ~/.local/share/pickle-rick/sessions/2026-05-01-325ccb80/*/linear_ticket_*.md; do grep -E "^title|^status" $d; done`)
- 29 commits since launch sha `019b520` (last pre-launch sha)
- Pipeline phases: pickle ✓, citadel ✓, anatomy-park ✗ (deploy-reversion → gate-baseline), szechuan-sauce never ran
- tmux session is dead; safe to kill any leftover `tmux kill-session -t pipeline-325ccb80`

## Active queue (priority order — see MASTER_PLAN §Active Queue for full table)

1. **`prds/p2-bundle-deploy-reversion-and-gate-baseline-diagnostic.md`** ⭐ **NEW P0 BUNDLE** — composes:
   - F7 lockdown from `schema-version-deploy-reversion-rca.md` (was deferred; promoted to mandatory)
   - Gate-baseline runtime diagnostic (verify recapture fix actually fires once deploy persists)
   - Section C from `pipeline-runner-state-active-not-claimed-on-relaunch.md` (P3, pulled forward)
   - Section D from `readiness-gate-manifest-prd-bundle-mismatch.md` (P2, pulled forward)
   - ~12-15 atomic tickets, ~800 LOC, target v1.68.0
2. **`prds/hermes-integration.md`** — ready to refine (P2)
3. **`prds/multi-repo-task-state-drift.md`** — refined draft (P2)
4. **`prds/god-functions-remediation-phase-2.md`** — refactor epic
5. **`prds/deepseek-integration.md`** — third backend

## Bugs that surfaced today (all logged)

| PRD | Status | Notes |
|---|---|---|
| `prds/p1-bug-bundle-2026-05-01-pm.md` | **20/20 tickets shipped** as commits 47904e7..2c814e8; anatomy-park failed downstream of deploy-reversion | Bundle's own work landed; verification phase blocked by meta-bug |
| `prds/readiness-gate-manifest-prd-bundle-mismatch.md` | Draft (P2) | Logged 2026-05-01 PM |
| `prds/pipeline-runner-state-active-not-claimed-on-relaunch.md` | Draft (P3) | Logged 2026-05-01 PM |
| `prds/p2-bundle-deploy-reversion-and-gate-baseline-diagnostic.md` | **Draft (P0) ⭐ NEW** | Logged just now; bundles F7 lockdown + 3 follow-ups |

## What broke 4× today (deploy-reversion meta-bug)

`bash install.sh` deploys correctly → `gh release latest` returns wrong tarball OR `check-update.js` auto-update reverts → deployed reverts to older version within ~30-60 min → next pipeline subprocess loads stale JS → anatomy-park fails on a bug that was already fixed in source.

**Operator workaround until P0 bundle ships**:
```bash
# Pre-flight before EVERY pipeline launch
SRC_V=$(jq -r .version /Users/gregorydickson/loanlight/pickle-rick/pickle-rick-claude/extension/package.json)
DEP_V=$(jq -r .version $HOME/.claude/pickle-rick/extension/package.json)
[ "$SRC_V" = "$DEP_V" ] || bash install.sh
# Then launch within ~5 min
```

## Recommended next actions (after `/clear`)

1. **Verify deploy state**: run the pre-flight above. If drift, `bash install.sh`.
2. **Decide on v1.67.0 tag**: closer commit `2c814e8` bumped source to 1.67.0 but it isn't tagged on GitHub. Either tag it now (risky — may get reverted) OR wait for the P0 bundle to ship F7 lockdown first, then tag v1.67.0 + v1.68.0 together.
3. **Launch the P0 bundle**: `/pickle-refine-prd prds/p2-bundle-deploy-reversion-and-gate-baseline-diagnostic.md` then `/pickle-pipeline ... --backend codex`.
4. **Watchdogs are stopped**: jobs `bcdd4a30` and `2083193a` cancelled; no scheduled work running. Re-arm if launching another long pipeline.

## Key file refs

- MASTER_PLAN: `prds/MASTER_PLAN.md` (160 lines, lean operational dashboard)
- v1.67.0 closer: commit `2c814e8`
- Bundle session artifacts: `~/.local/share/pickle-rick/sessions/2026-05-01-325ccb80/`
- Today's session log analysis lives in this CONTEXT file (delete after read)

## Skills used today

- `/pickle-pipeline` (with refinement)
- `/pickle-refine-prd` (called inline by /pickle-pipeline)
- `/loop` (for the watchdog cron — both stopped)

## Don't forget

- Working tree was clean when this file was written. If it's dirty when you read this, the auto-updater MAY have written something — investigate before committing.
- `prds/CLAUDE.md` and `extension/CLAUDE.md` are committed source-of-truth — don't edit deployed copies in `~/.claude/pickle-rick/`.
